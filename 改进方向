 四个参考项目的自动编码业务逻辑分析报告

  源码已克隆至 D:\zthree2\reference_projects\，以下是详细分析。

  ---
  1. QualCoder（最相似的 PyQt5+Python QDA 工具）

  架构： PyQt6 + LangChain + FAISS + LLM（多供应商）

  核心自动编码流程（三段式）：

  第一阶段：语义扩展
    └─ 给定一个代码名称+备忘录 → LLM生成10个同义词/近义表述 → 扩展语义覆盖面

  第二阶段：语义检索
    └─ 10个描述逐个在FAISS向量库中做相似度搜索 → 每个返回top50相关文本块

  第三阶段：交叉排序+逐块分析
    └─ 合并去重 → Reciprocal Rank Fusion排序（出现频次×相似度得分）
    └─ LLM逐块判断"相关/不相关" → 输出: {interpretation, related: bool, quote}
    └─ 用Smith-Waterman算法精确定位引文在原文中的位置

  关键技术决策：
  - 使用 intfloat/multilingual-e5-large（1024维）做本地嵌入，支持多语言
  - FAISS 向量库存储所有文档的分块（500字符+100重叠）
  - E5模型区分 passage: 和 query: 前缀，提升检索精度
  - JSON Schema强制输出结构化结果，json_repair 容错解析
  - Prompt体系分为 search/code_analysis/topic_analysis/text_analysis 四类，可自定义

  ---
  2. iQual（世界银行，BERT/sklearn方案）

  架构： scikit-learn Pipeline + sentence-transformers/spaCy + 分类器

  核心自动编码流程：

  人工编码小样本 (100-200条)
    → sentence-transformers/BERT 向量化
    → 特征缩放/降维 (UMAP/PCA/TruncatedSVD)
    → sklearn分类器训练 (SVM/LogisticRegression/RandomForest等)
    → 预测剩余数千条数据
    → BinaryThresholder: 自动搜索最优分类阈值（F1最大化）

  关键技术决策：
  - 不依赖LLM API，完全本地运行
  - Pipeline模式：Input → Vectorizer → Scaler → Reducer → Classifier → Thresholder
  - 支持 GridSearchCV 超参数搜索 + 交叉验证
  - 独立的评估模块：精确率/召回率/F1/混淆矩阵
  - 支持自定义嵌入字典（预计算嵌入，加速推理）

  ---
  3. LLMCode（Aalto大学，LLM+聚类方案）

  架构： OpenAI API + UMAP + HDBSCAN + 归纳/演绎双模式

  核心自动编码流程（归纳式）：

  (1) 构造Prompt: 编码指令 + few-shot示例 + 已有代码列表
      → 随机打乱顺序消解LLM的recency bias
  (2) 批量发送 → LLM返回带标注的文本: **原文片段**<sup>代码1; 代码2</sup>
  (3) 错误修正: 编辑距离校验 → fuzzy matching重建标注位置
  (4) 代码提取: 解析**...**<sup>...</sup>格式 → 拆分为单个代码
  (5) 代码分组: embedding(代码) → UMAP降维 → HDBSCAN聚类 → 主题生成
  (6) 演绎式: 给定codebook → 约束LLM只能使用已有代码 → 自动去除幻觉代码

  关键技术决策：
  - 两种Prompt构建方式：construct_inductive_prompt / construct_deductive_prompt
  - 幻觉检测：_remove_code() - 自动删除LLM生成的不在codebook中的代码
  - 代码一致性：顺序处理+维护已有代码描述，新文本编码时会看到之前生成的代码
  - 代码分组两段式：(1) LLM先提取主题 → (2) LLM将代码分配到主题
  - 缓存机制：所有LLM调用都做pickle缓存，节省API费用

  ---
  4. cognitum（LMQL约束生成方案）

  架构： LMQL（Language Model Query Language）+ 约束解码

  核心自动编码思路：

  定义: prompt模板({text}占位符) + valid_labels列表
    → LMQL的argmax约束解码：强制输出必须在valid_labels内
    → 输出格式: text|label1, label2（管道符分隔）
    → 评估: exact match / partial match / false positives / false negatives

  关键技术决策：
  - 约束生成是核心创新：用LMQL的语法强制LLM输出只能在预定义标签中选择
  - 避免LLM幻觉出的不存在的标签
  - 支持置信度分数（虽然目前是占位实现）
  - 批量处理：拼接多条文本到单个prompt中

  ---
  5. 各项目对比与 zthree2 可借鉴的优点

  ┌────────────────┬──────────────┬─────────────────────────┬─────────────────────┬──────────────────┬──────────────┐
  │      维度      │  QualCoder   │          iQual          │       LLMCode       │     cognitum     │ zthree2 现状 │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 模型方式       │ LLM API      │ 本地BERT+sklearn        │ LLM API             │ LLM+LMQL约束     │ 本地BERT     │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 多级编码       │ 单级         │ 单级                    │ 单级→聚类分组       │ 单级             │ 三级编码     │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 检索增强       │ FAISS+RAG    │ 无                      │ 无                  │ 无               │ 无           │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 代码一致性     │ 代码描述扩展 │ 分类器复用              │ 已有代码列表        │ valid_labels约束 │ 候选评分     │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 可自定义Prompt │ 四级分类体系 │ 无                      │ coding_instructions │ prompt模板       │ 无           │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 幻觉处理       │ JSON Schema  │ N/A(非LLM)              │ 编辑距离校验        │ LMQL约束         │ N/A(非LLM)   │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 阈值优化       │ 无           │ BinaryThresholder       │ 无                  │ 无               │ 硬阈值       │
  ├────────────────┼──────────────┼─────────────────────────┼─────────────────────┼──────────────────┼──────────────┤
  │ 评估体系       │ 无           │ 完整(F1/混淆矩阵/Kappa) │ Hausdorff距离       │ Exact/Partial    │ 准确率       │
  └────────────────┴──────────────┴─────────────────────────┴─────────────────────┴──────────────────┴──────────────┘

  ---
  6. 针对 zthree2 的具体优化建议

  建议1：引入 RAG 检索增强（借鉴 QualCoder）
  - 用现有的 bge-small-zh-v1.5 构建文档向量库（FAISS），对所有访谈文本做分块嵌入
  - 在自动编码前先语义检索相关上下文，减少BERT候选评分的盲目性
  - 预估提升：减少50%无关候选，提升编码速度

  建议2：Prompt模板体系（借鉴 QualCoder + LLMCode）
  - 一阶编码的prompt可参考 QualCoder 的搜索类prompt（Focused/Open Search）
  - 增加"编码指令"配置项，让用户自定义编码策略
  - 如已有代码列表传参，可参照 LLMCode 的 codes_so_far 机制增强一致性

  建议3：动态阈值优化（借鉴 iQual）
  - 当前一阶编码使用固定阈值决定是否采纳候选代码
  - iQual的 BinaryThresholder 自动搜索F1最优阈值，可移植此方案
  - 定期从人工编码数据中校准阈值

  建议4：编码幻觉防护（借鉴 LLMCode + cognitum）
  - 如果未来接入LLM辅助编码，LLMCode的 _remove_code() 和 cognitum 的 valid_labels 约束是关键
  - 当前BERT方案不需要，但若引入生成式模型则必须

  建议5：评估体系完善（借鉴 iQual）
  - 增加 F1 score、精确率、召回率指标（当前仅有准确率）
  - 增加 Krippendorff's alpha 作为编码者间信度指标

  建议6：代码分组升级（借鉴 LLMCode）
  - 当前的一阶→二阶→三阶通过统计共同出现频率升级
  - LLMCode的 UMAP + HDBSCAN 聚类方案可作为二阶编码的辅助手段

● ---
  总结

  四个参考项目中，QualCoder
  与本项目最相似（同为PyQt+Python的QDA桌面应用），其三段式RAG编码流程（语义扩展→FAISS检索→LLM逐块分析）最值得借鉴。iQual
   的sklearn Pipeline架构和阈值优化机制可直接移植到zthree2的BERT评分环节。LLMCode
  的编码一致性机制（已有代码列表、幻觉检测、fuzzy matching纠错）为未来接入LLM做好了设计参考。cognitum
  的约束生成方案为结构化输出提供了可靠保障。

  性价比最高的三个改进方向：RAG检索增强 > 动态阈值优化 >
  评估体系完善。这三项都不需要改变现有BERT架构，实现成本低但收益显著。